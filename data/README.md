# Data Directory

Place local AIS JSONL files or processed datasets here when running experiments.

The repository does not track raw AIS data, generated `.npz` caches,
checkpoints, or experiment logs. Use the preprocessing and training commands in
the project README to regenerate local artifacts.
